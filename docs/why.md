# Why graph-harness

> **Work in progress.** This page will cover the motivation behind graph-harness — the ecosystem context (the AGENTS.md-as-encyclopedia problem, agent-side harness silos, the gap between LSP and committed indexes), the key design choices (substrate as a layered graph, region-scoped pinning, one trust pipeline for every claim), and the bet underlying the project.
