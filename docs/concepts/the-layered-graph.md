# The layered graph

> **Work in progress.** This page will describe the layered substrate — what each layer holds (`source.live`, `code.core`, `code.framework`, `semantic.overlay`, `change.process`, `review.queue`, `history.evolution`), where each layer's facts come from, the single global sequence that coordinates them, the freshness contract, and the trust matrix governing cross-layer queries.
