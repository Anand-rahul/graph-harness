# Lifecycle attachment

> **Work in progress.** This page will describe the activation rule — when and how a harness attaches its context, rules, registered tools, and repair recipes to an operating context (explore, plan, edit, validate, on-PR, finish), the difference between *touched* and *impacted* entities, and how relation-aware traversal decides which harnesses wake.
